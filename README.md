#表情选择组件

##预计功能
1. 任意点击一个元素，就在这个元素的下方显示该组件

2. 选完表情后触发回调事件，包含选择的标签的代码
3. 传入的参数，包含所有表情图片的数组，每一个表情是有表情代码和表情实际图片链接组成，如果是emoji另说，我还不知道。
4. 可传输数组，则表示有多种类型的表情
5. 添加自己的标签图片，并显示在个人收藏中
6. 点击非该组件区域则关闭该组件

###关于命名
为了给每个组件定义自己的命名空间，我会统一一个组件的前缀，但难免会有重复的，
现在我在想是不是做一个工具，一开始设定一个参数作为前缀，然后使用gulp处理添加自定义的前缀